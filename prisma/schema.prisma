generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Engine {
  id           Int    @id @default(autoincrement())
  name         String @unique
  displacement Int
  horsepower   Int
  torque       Float

  @@map("engine")
}
